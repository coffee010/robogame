# Raspberry Pi runtime environment.
# Recommended venv command on Raspberry Pi OS:
#   python3 -m venv --system-site-packages .venv
#   . .venv/bin/activate
#   python -m pip install -r requirements-pi.txt
#
# Install OS packages first when using the Pi camera stack:
#   sudo apt update
#   sudo apt install -y python3-picamera2 libcamera-apps alsa-utils
#
# Default Pi install: play pre-generated WAV files and run hardware/UI code.
-e .[pi,ui]
#
# Optional: uncomment this if the Pi must generate Chinese Piper speech live.
# This installs torch and is much larger/slower.
# -e .[tts-zh]
